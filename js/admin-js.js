(function($, document, window) {
	
	$( document ).ready(function(){

		var $publishBtn = $( '#publish' ),
			btnText 	= $publishBtn.attr( 'name' );

		if ( btnText == 'save' ) {
			// $publishBtn.remove();
		}

	});

}(jQuery, document, window));